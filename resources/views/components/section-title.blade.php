@props(['title'])

<div class="flex items-center justify-between my-4 pb-2 px-10">
    <h1 style="color: var(--text-primary);" class="text-xs md:text-md lg:text-lg xl:text-2xl font-black">{{ $title }}</h1>
</div>
